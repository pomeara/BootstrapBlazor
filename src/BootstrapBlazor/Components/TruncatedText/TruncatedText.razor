@namespace BootstrapBlazor.Components
@inherits BootstrapComponentBase

@* Truncated text with the full value exposed as the title tooltip. *@
@if (!string.IsNullOrEmpty(Value))
{
    <span @attributes="AdditionalAttributes" title="@Value">@DisplayText</span>
}
